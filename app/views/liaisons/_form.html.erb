<%= form_for(@liaison) do |f| %>
  <% if @liaison.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@liaison.errors.count, "error") %> prohibited this liaison from being saved:</h2>

      <ul>
      <% @liaison.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :relation %><br>
    <%= f.text_area :relation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
